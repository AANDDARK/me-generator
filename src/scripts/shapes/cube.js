export class Cube{
    sides = [];
    constructor(){
        if(this.sides.every(value => value === sides[0])) {

        } else{
            throw Error("cube need have all sides same");
        }
    }
    CheckRules(){
        
    }
}